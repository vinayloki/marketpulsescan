# ── GitHub Actions dependencies only ──────────────────────────────────
# streamlit is intentionally EXCLUDED — local dev tool only.
# fastapi is intentionally EXCLUDED — export_static_api.py no longer imports it.
# ──────────────────────────────────────────────────────────────────────
pandas>=2.0
numpy>=1.26
requests>=2.31
yfinance>=0.2.40
feedparser>=6.0
pyarrow>=14.0
sqlalchemy>=2.0
scikit-learn>=1.4
